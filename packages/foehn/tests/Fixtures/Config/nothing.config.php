<?php

declare(strict_types=1);

// A config file has to return the object it configures; this one returns nothing
// useful, and the loader has to skip it rather than register a string.
return 'not a config object';
